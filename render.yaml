services:
  - type: web
    name: slns-website
    env: node
    plan: starter
    healthCheckPath: /health
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
